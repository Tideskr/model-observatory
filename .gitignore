# The archived detector remains excluded because it contains a captured raw
# HTTP request. The two scoring baselines below are included with the original
# author's permission and are the only Legacy files required by the backend.
Legacy/*
!Legacy/gpt56_vnext/
Legacy/gpt56_vnext/*
!Legacy/gpt56_vnext/baselines/
Legacy/gpt56_vnext/baselines/*
!Legacy/gpt56_vnext/baselines/runtime_catalog.json
!Legacy/gpt56_vnext/baselines/trusted_likelihood_v2.json

# Local tool settings
.claude/settings.local.json

# Dependencies & build output
node_modules
dist
dist-ssr
coverage

# Backend runtime state and local secrets
backend/.env
backend/.data/

# Logs
logs
*.log

# OS / editor
.DS_Store
.idea
.vscode/*
!.vscode/extensions.json
*.local

# Python local runner runtime state
__pycache__/
*.py[cod]
local-runner/gpt56_vnext_runs/
