# Model Observatory Frontend

React + TypeScript + Vite 前端原型。当前使用 `src/data.ts` 中的模拟数据，不连接后端，也不会发送或保存 API key。

```bash
npm install
npm run dev
```

可用检查：

```bash
npm run lint
npm run build
```

产品、安全和社区治理文档位于仓库根目录的 `docs/`。
